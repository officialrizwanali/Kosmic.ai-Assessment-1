const uploadDir = "uploads/";

const VIDEO_DURATION_LIMIT = 300; // 5 minutes

const VIDEO_FILE_SIZE_LIMIT = 100 * 1024 * 1024; // 100MB

module.exports = {
  uploadDir,
  VIDEO_DURATION_LIMIT,
  VIDEO_FILE_SIZE_LIMIT,
};
